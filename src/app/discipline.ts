export interface Discipline {
  id: number;         // Identificador único
  name: string;       // Nome da disciplina
  code: string;       // Código da disciplina (ex.: MAT101)
  workload: number;   // Carga horária em horas
  description: string;// Descrição ou ementa
}
